name: forcefields-dev
channels:
  - conda-forge
dependencies:
  - bump2version
  - foyer
  - pytest
  - pytest-cov
  - pre-commit
  - pip
  - gmso >= 0.9.0
  - pip:
      - "--editable=git+https://github.com/rsdefever/antefoyer.git#egg=antefoyer"
